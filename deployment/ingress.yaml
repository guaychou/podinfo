apiVersion: networking.k8s.io/v1
metadata:
  name: dns-ingress
  namespace: default
spec:
  ingressClassName: nginx
  rules:
  - host: podinfo.perf.lokal
    http:
      paths:
      - path: /
        backend:
          serviceName: podinfo
          servicePort: 80